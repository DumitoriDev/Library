<mah:MetroWindow x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:libraryClass="clr-namespace:LibraryClass;assembly=LibraryClass"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition>

            </RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>

            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <mah:MetroAnimatedTabControl x:Name="tabControl"  Margin="20.8,0,23.6,0" Grid.Column="1">
            <TabItem x:Name="tabBooks" Header="Книги">
                <Grid>
                    <DataGrid Name="GridBooks"  ItemsSource="{Binding Path=Books}" Margin="10,20,10,0"
                              AutoGenerateColumns="False"
                              Style="{StaticResource AzureDataGrid}" SelectionChanged="GridBooks_OnSelectionChanged">
                        <DataGrid.Columns>





                            <DataGridTemplateColumn Header="Image">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Height="25" Width="50" Source="{Binding Img.Source}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Название книги"
                                                Binding="{Binding Name}"/>

                            
                            <DataGridTextColumn Header="Количество страниц"
                                                Binding="{Binding Count}" />

                            <DataGridTextColumn Header="Цена"
                                                Binding="{Binding Price}" />
                        


                        </DataGrid.Columns>
                       
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem x:Name="tab2" Header="Читатели">
                <TextBlock Text="hello1"></TextBlock>
            </TabItem>
            <TabItem x:Name="tab3" Header="Должники">
                <CheckBox x:Name="check3" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            </TabItem>
            <TabItem Header="image">
                
            </TabItem>
        </mah:MetroAnimatedTabControl>

       
       

</Grid>
</mah:MetroWindow>
